#include <string>
#include <vector>
#include <algorithm>

std::vector<std::string> mprec(std::string message);

std::string mpsen(std::vector<std::string> result);

bool isNumber(const std::string& s);
